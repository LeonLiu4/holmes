# ChatLog GPT Analyzer

This project scrapes chat logs from a webpage and analyzes them using OpenAI GPT (like GPT-4o).

## 🔧 Setup

1. Clone the repo:
```bash
git clone https://github.com/yourusername/chatlog-gpt-analyzer.git
cd chatlog-gpt-analyzer
